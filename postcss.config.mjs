// PostCSS config for Next.js
// WARNING: This file may be picked up by Strapi Cloud builds from root directory
// If that causes issues, ensure strapi/postcss.config.mjs exists and takes precedence

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
